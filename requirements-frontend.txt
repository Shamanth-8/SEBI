# Dashboard-only dependencies, for the split two-service Render deploy.
#
# frontend/ talks to the API over HTTP and imports nothing from backend/, so it
# needs none of the server or ML stack. Dropping fastapi, uvicorn, langchain,
# langgraph, anthropic, openai, faiss-cpu, scikit-learn, apscheduler and
# reportlab is what keeps the dashboard inside a free 512MB instance.
#
# Versions are pinned identically to requirements.txt — keep them in sync.
streamlit==1.40.2
streamlit-option-menu==0.3.13
plotly==5.24.1
pandas==2.2.3
numpy==2.2.0
httpx==0.28.0

# Imported lazily inside dashboard.py, but required once those views are opened:
# pdfplumber (page preview), pyvis + networkx (graph rendering).
pdfplumber==0.11.4
pyvis==0.3.2
networkx==3.4.2
